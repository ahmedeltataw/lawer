---
export interface props {
    title:string,
    url:string,
    icon:string,
    classes:string,
}
const {title , url , icon , classes} = Astro.props
---
<a href={url} class:list={classes}> {title} <i class={icon} ></i></a>
<!--  -->